extends layout  

include mixins/crearProyecto 

block contenido
    .contenedor
        h1.contenedor__titulo #{nombrePagina}

        if errores 
            each error in errores
                .alerta #{error.texto}
        +crearProyecto(proyecto)